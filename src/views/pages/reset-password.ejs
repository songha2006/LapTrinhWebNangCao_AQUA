<div class="max-w-md mx-auto px-4 py-14">
  <div class="bg-white rounded-3xl border border-stone-200 p-8">
    <h1 class="text-2xl font-bold tracking-tight text-center">Đặt lại mật khẩu</h1>
    <p class="text-sm text-stone-500 text-center mt-1 mb-6">Nhập mật khẩu mới cho tài khoản của bạn</p>

    <% if (errors.length) { %>
      <div class="bg-red-50 border border-red-200 text-red-700 rounded-xl px-4 py-3 mb-5 text-sm">
        <ul class="list-disc list-inside space-y-0.5">
          <% errors.forEach(e => { %><li><%= e.msg %></li><% }) %>
        </ul>
      </div>
    <% } %>

    <form action="/reset-password/<%= token %>" method="POST" class="space-y-4">
      <div>
        <label class="block text-sm font-medium mb-1.5" for="password">Mật khẩu mới</label>
        <input id="password" type="password" name="password" required minlength="6"
               class="w-full rounded-xl border border-stone-300 px-4 py-2.5 text-sm outline-none focus:border-brand-500">
        <p class="text-xs text-stone-400 mt-1">Tối thiểu 6 ký tự</p>
      </div>
      <div>
        <label class="block text-sm font-medium mb-1.5" for="password_confirm">Xác nhận mật khẩu mới</label>
        <input id="password_confirm" type="password" name="password_confirm" required minlength="6"
               class="w-full rounded-xl border border-stone-300 px-4 py-2.5 text-sm outline-none focus:border-brand-500">
      </div>
      <button class="w-full py-3 rounded-full bg-brand-600 text-white font-semibold hover:bg-brand-700">Xác nhận</button>
    </form>
  </div>
</div>
